return {
  "folke/noice.nvim",
  opts = {
    presets = {
      bottom_search = true,        -- classic search bar at the bottom
      command_palette = true,      -- cmdline + popupmenu together, centered
      long_message_to_split = true, -- long messages go to a split instead of a popup
      inc_rename = true,           -- live rename preview in the cmdline
    },
  },
}
